spring:
  application:
    name: auth
  data:
    mongodb:
      host: mongo-auth
      port: 27017
      username: admin
      password: passadmin
      authentication-database: admin
      database: authdb
      auto-index-creation: true
  profiles:
    active: docker

auth:
  token:
    secret: ${AUTH_TOKEN_AES256_KEY}
